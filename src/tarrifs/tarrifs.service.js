import { Tarrif, Company } from '../models';

export const getTarrif = async ({ id }) => {
    try {
        
    } catch (error) {
        
    }
}

export const getCompanyTarrifs = async (args, context) => {
    try {
        
    } catch (error) {
        
    }
}

export const getTarrifs = async (args, context) => {
    try {
        
    } catch (error) {
        
    }
}

export const editTarrif = async (args, context) => {
    try {
        
    } catch (error) {
        
    }
}
